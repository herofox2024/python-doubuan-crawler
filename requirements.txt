# 打包所需依赖列表
# 请确保安装以下Python包后再进行打包

# 核心依赖
requests>=2.25.0
beautifulsoup4>=4.9.0
pandas>=1.3.0
tqdm>=4.60.0

# GUI依赖（通常Python自带）
# tkinter - Python标准库

# 数据库依赖（通常Python自带）
# sqlite3 - Python标准库

# 打包工具
pyinstaller>=4.5

# 可选依赖（用于更好的兼容性）
openpyxl>=3.0.0  # Excel文件支持
lxml>=4.6.0      # XML解析器